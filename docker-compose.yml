services:
  postgres:
    image: postgres:16-alpine
    container_name: leadpulse-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-leadpulse_db}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-leadpulse_db}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - leadpulse-network

  redis:
    image: redis:7-alpine
    container_name: leadpulse-redis
    restart: unless-stopped
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - leadpulse-network

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local

networks:
  leadpulse-network:
    driver: bridge
