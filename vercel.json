{
  "framework": null,
  "buildCommand": "echo 'Static site - no build step required'",
  "outputDirectory": ".",
  "cleanUrls": true,
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
